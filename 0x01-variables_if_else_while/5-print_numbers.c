#include <stdio.h>
/**
 * main- Entry point
 * Description: prints all singl digits numbers
 * Return: always (0)
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);

}
